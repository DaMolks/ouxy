<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Ouxy" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs primaires de l'application -->
        <item name="colorPrimary">@color/space_primary</item>
        <item name="colorPrimaryVariant">@color/space_primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Couleurs secondaires -->
        <item name="colorSecondary">@color/space_accent</item>
        <item name="colorSecondaryVariant">@color/space_accent_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Statut et barre de navigation -->
        <item name="android:statusBarColor">@color/space_primary</item>
        <item name="android:navigationBarColor">@color/space_primary</item>

        <!-- Icônes de la barre de statut en blanc -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Style global -->
        <item name="android:windowBackground">@color/space_background</item>
    </style>
</resources>